# 전북교육청 제도맵 (korea100 기반 포크)

원본: hosungseo/korea100 (MIT License) — 코드는 그대로 두고 데이터만 전북교육청 업무로 교체한 포크입니다.

## 현재 담긴 것
- 파일럿 제도 1건: **학생맞춤통합지원** (전북특별자치도교육청 조례 제5964호 근거)
- `web/data/institutions/student-tailored-integrated-support.json`
- `docs/institutions-100-manifest.json` (단일 항목)
- 원본 데이터 검증(`validate:data`) 통과 완료

## 로컬에서 실행
```
cd web
npm install
npm run dev
```
브라우저에서 http://localhost:3000 접속.

## 정적 사이트 빌드(배포용)
```
cd web
npm run build      # 결과물: web/out/
```

## 제도 추가하는 법
1. `web/data/institutions/{slug}.json` 새 파일 작성 (기존 파일럿을 템플릿으로)
2. `docs/institutions-100-manifest.json`에 항목 추가 (priority는 1..N 연속)
3. `cd web && npm run validate:data` 로 검증
4. `npm run dev` 로 확인

## 브랜딩
제목·설명은 `web/src/app/layout.tsx` 등에서 '전북교육청 제도맵'으로 변경됨.
